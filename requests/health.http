### Health and Liveness
# Use VS Code's REST Client extension: click "Send Request" above any block.

@base = http://localhost:3000

### Liveness probe — always returns 200 while the process is up
GET {{base}}/api/health/live

### Readiness probe — returns 200 only if DB + Redis are reachable
GET {{base}}/api/health
