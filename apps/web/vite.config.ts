import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      // Forward API calls to the backend in dev so the browser can use relative URLs.
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true,
      },
      // Forward Socket.IO to the backend (WebSocket upgrade support).
      '/socket.io': {
        target: 'http://localhost:3000',
        changeOrigin: true,
        ws: true,
      },
    },
  },
  build: {
    outDir: 'dist',
    sourcemap: true,
  },
});
